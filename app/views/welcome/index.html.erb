<body class="welcome-body">
<div class="welcome-locales">
  <%= link_to params.merge(locale: 'en') do %>
      <i class="famfamfam-flag-gb"></i> UK
  <% end %>
  <%= link_to params.merge(locale: 'uk') do %>
      <i class="famfamfam-flag-ua"></i> UA
  <% end %>
</div>
<div class="container site-wrapper">
  <div class="jumbotron">
    <div class="text-center inner">
      <img src="/assets/logo-big.png"/>

      <h1 class="cover-heading"> <%= t('.slogan') %></h1>

      <p class="lead"> <%= t('.short_decription') %> </p>

      <p class="lead">
        <%= link_to t('.button'), { controller: 'films', action: 'index' }, class: 'btn btn-lg btn-default' %>
      </p>

      <!-- Just to remember-->
      <div class="counter">
        <h3>Estimated Time Remaining Before Launch:</h3>

        <div id="countdown">
        </div>
      </div>

      <script type="text/javascript" charset="utf-8">
          // set the date we're counting down to
          var target_date = new Date('Jun, 15, 2015').getTime();

          // variables for time units
          var days, hours, minutes, seconds;

          // get tag element
          var countdown = document.getElementById('countdown');

          // update the tag with id "countdown" every 1 second
          setInterval(function () {

              // find the amount of "seconds" between now and target
              var current_date = new Date().getTime();
              var seconds_left = (target_date - current_date) / 1000;

              // do some time calculations
              days = parseInt(seconds_left / 86400);
              seconds_left = seconds_left % 86400;

              hours = parseInt(seconds_left / 3600);
              seconds_left = seconds_left % 3600;

              minutes = parseInt(seconds_left / 60);
              seconds = parseInt(seconds_left % 60);

              // format countdown string + set tag value
              countdown.innerHTML = '<span class="days">' + days + ' <b>Days</b></span> <span class="hours">' + hours + ' <b>Hours</b></span> <span class="minutes">'
                      + minutes + ' <b>Minutes</b></span> <span class="seconds">' + seconds + ' <b>Seconds</b></span>';

          }, 1000);
      </script>

      <!--<div class="welcome-footer">-->
      <!--<hr>-->
      <!--<p class='text-center' style="color: #fff;">Video Depot <a href="http://getbootstrap.com" style="color: gray;">Maksym Tymoshyk</a>. &copy;2015-->
      <!--</p>-->
      <!--</div>-->
    </div>
  </div>
</div>
</body>